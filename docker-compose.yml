version: '3.8'

services:
  postgres-dev:
    image: postgres:alpine
    container_name: quakelog-dev
    environment:
      POSTGRES_USER: quakelog
      POSTGRES_PASSWORD: quakelog
      POSTGRES_DB: quakelog
    ports:
      - 5432:5432

  minio:
    image: minio/minio:RELEASE.2024-01-18T22-51-28Z
    command: server --console-address ":9001" /data
    container_name: minio
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: quakelog
      MINIO_ROOT_PASSWORD: quakelog
      MINIO_ACCESS_KEY: UwdFk3p5N8cIqU9Jf6Do
      MINIO_SECRETE_ACCESS: PUXUX68YnB1YECJXZUKsPX5k04zeaNnMDFpCXsWT
    volumes:
      - ./minio-data:/data